<custom-navigation title="nav" showBtn="0" />
<view class="container nav-container">
  <navigator class="nav-item" wx:for="{{navs}}" wx:key="{{index}}" url="{{item.url}}">
    {{item.title}}
  </navigator>
</view>